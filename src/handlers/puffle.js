'use strict'

/**
 * @exports
 */
module.exports = {
  /**
   * Retrieve puffles by player id
   * @param {Array} data
   * @param {Penguin} penguin
   */
  getPufflesByPlayerId: (data, penguin) => {
    // Todo, need this to join igloo
    penguin.sendXt('pg', 750)
  }
}
