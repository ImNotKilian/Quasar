'use strict'

/**
 * @exports
 */
module.exports = {
  /**
   * Handle the join server request
   * @param {Array} data
   * @param {Penguin} penguin
   */
  handleJoinServer: (data, penguin) => {

  }
}
